import React from 'react'
import styled from "styled-components/macro";

const StyledBottomSlider = styled.div<any>`

`

const BottomSlider = ({...props}) => {
  return <StyledBottomSlider {...props}/>
}

export default BottomSlider